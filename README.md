
# Timelyx — Starter Netlify (Functions + Background + Scheduled)

Ce projet est prêt pour **Netlify** avec des fonctions serverless qui agissent comme des “agents” :

- **`ping.js`** : test simple (GET)
- **`run.js`** : exécution à la demande (POST)
- **`run-background.js`** : tâche longue (background)
- **`run-scheduled.mjs`** : tâche planifiée (cron)

---

## 📂 Structure
